import { defineConfig } from 'vite';\nimport { svelte } from '@sveltejs/vite-plugin-svelte';\nimport { resolve } from 'path';\n\n// https://vitejs.dev/config/\nexport default defineConfig({\n  plugins: [svelte()],\n  resolve: {\n    alias: {\n      '@': resolve(__dirname, 'src'),\n    },\n  },\n  build: {\n    rollupOptions: {\n      input: 'src/main.ts',\n      preserveEntrySignatures: 'strict',\n      output: {\n        entryFileNames: 'travel-booking-admin.js',\n        format: 'system',\n        // Dependencias externas que no deben ser incluidas en el bundle\n        globals: {\n          // Agregamos las dependencias compartidas\n          'svelte': 'Svelte',\n          '@travel-booking/shared-lib': 'SharedLib'\n        }\n      },\n      external: [\n        'svelte',\n        '@travel-booking/shared-lib'\n      ]\n    }\n  },\n  server: {\n    port: 5083\n  }\n});